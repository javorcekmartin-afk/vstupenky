let awayLogo='';const $=id=>document.getElementById(id);const els={op:$('opponent'),dt:$('date'),tm:$('time'),sn:$('startNo'),ct:$('count'),pp:$('perPage'),fs:$('fontSize'),ns:$('numberSize'),lg:$('logoInput'),pages:$('pages')};function esc(s){return String(s).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;')}function fmtNo(n){return String(n).padStart(3,'0')}function ticket(num,op,dt,tm){const n=fmtNo(num);return `<div class="ticket"><div class="home"><img src="${window.HOME_LOGO}" alt="TJ Záhoran Kostolište"><div class="number-main">${n}</div></div><div class="match"><div class="home-name">TJ ZÁHORAN KOSTOLIŠTE</div><div class="vs">VS.</div><div class="away-name">${esc(op)}</div><div class="info"><div><div class="lbl">DÁTUM</div><div class="val">${esc(dt)}</div></div><div><div class="lbl">ČAS</div><div class="val">${esc(tm)}</div></div></div></div><div class="away">${awayLogo?`<img src="${awayLogo}" alt="Logo súpera">`:''}</div><div class="stub"><div class="num">${n}</div></div></div>`}function generate(){const op=els.op.value.trim()||'SÚPER',dt=els.dt.value.trim(),tm=els.tm.value.trim(),sn=Math.max(1,parseInt(els.sn.value||1,10)),ct=Math.max(1,Math.min(1000,parseInt(els.ct.value||150,10))),pp=parseInt(els.pp.value||12,10),fs=els.fs.value||'normal',ns=els.ns.value||'large';els.pages.innerHTML='';for(let p=0;p<Math.ceil(ct/pp);p++){const page=document.createElement('div');page.className='page '+(pp===6?'mode6':pp===10?'mode10':'mode12')+' fs-'+fs+' num-'+ns;let html='';for(let i=0;i<pp;i++){const ix=p*pp+i;if(ix<ct)html+=ticket(sn+ix,op,dt,tm)}page.innerHTML=html;els.pages.appendChild(page)}}els.lg.addEventListener('change',ev=>{const f=ev.target.files&&ev.target.files[0];if(!f)return;const r=new FileReader();r.onload=x=>{awayLogo=x.target.result;generate()};r.readAsDataURL(f)});$('generateBtn').onclick=generate;$('printBtn').onclick=()=>{generate();setTimeout(()=>window.print(),120)};[els.op,els.dt,els.tm,els.sn,els.ct,els.pp,els.fs,els.ns].forEach(el=>el.addEventListener('change',generate));generate();